version 'unspecified'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile (
            [group: 'io.opentracing', name: 'opentracing-api', version: '0.30.0']
    )

    testCompile group: 'junit', name: 'junit', version: '4.12'
}
